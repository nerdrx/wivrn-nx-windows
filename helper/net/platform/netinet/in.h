/*
 * WiVRn NX - Windows port: shadow for <netinet/in.h>.
 *
 * sockaddr_in/sockaddr_in6, in_addr/in6_addr, in6addr_any, IPPROTO_* and the
 * IPv6 membership options all come from ws2tcpip.h/ws2ipdef.h under the same
 * names.
 */
#pragma once
#include "../win_net.h"
